<svg width="200" height="200" xmlns="http://www.w3.org/2000/svg">
  <rect width="200" height="200" fill="lightblue" />
  <text x="10" y="20" font-family="Arial" font-size="16" fill="black">Clique aqui</text>

  <script type="text/ecmascript">
    <![CDATA[
      // Função para exibir um alerta com o domínio do site
      function showDomainAlert() {
        alert("Domain: " + window.location.hostname);
      }

      // Adiciona um ouvinte de evento ao SVG para chamar a função showDomainAlert quando clicado
      var svg = document.documentElement;
      svg.addEventListener('click', showDomainAlert);
    ]]>
  </script>
</svg>
